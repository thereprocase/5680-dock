const {chromium}=require('./tooling/node_modules/playwright');const fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true,args:['--enable-webgl','--ignore-gpu-blocklist']});const p=await b.newPage({viewport:{width:1800,height:1200}});await p.goto('http://127.0.0.1:8876/');await p.waitForFunction(()=>window.showcaseReady);await p.evaluate(()=>window.explodeShowcase());await p.waitForTimeout(1200);const el=p.locator('#canvas');const box=await el.boundingBox();const png=await el.screenshot();const anchors=await p.evaluate(()=>window.diagramAnchors());let s=`<svg xmlns="http://www.w3.org/2000/svg" width="${box.width}" height="${box.height}" viewBox="0 0 ${box.width} ${box.height}"><image width="100%" height="100%" href="data:image/png;base64,${png.toString('base64')}"/>`;anchors.forEach((a,i)=>{const x=a.x*box.width,y=a.y*box.height;s+=`<circle cx="${x}" cy="${y}" r="17" fill="#fbf4df" stroke="#123f44" stroke-width="2"/><text x="${x}" y="${y+5}" text-anchor="middle" font-family="Arial" font-size="15" font-weight="bold" fill="#123f44">${i+1}</text>`});s+='</svg>';fs.writeFileSync(__dirname+'/../../docs/assets/exploded-diagram.svg',s);console.log(JSON.stringify({box,anchors}));await b.close()})();
